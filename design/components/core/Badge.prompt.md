Small uppercase mono pill (`Badge`) for release/exit-code labels, and a plain rounded tag (`Tag`) for filter chips like provider names.

```jsx
<Badge tone="verify">v0.1.0</Badge>
<Badge tone="check">MIT</Badge>
<Tag>anthropic</Tag>
```
